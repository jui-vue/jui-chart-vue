name: Deploy demo site to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: jui-chart-vue

      # jui-chart-vue depends on jui-graph-ts via "file:../jui-graph-ts" (it isn't
      # published to npm yet), so that sibling repo has to be checked out and built
      # here too, the same way local development already expects it to exist next
      # to this repo on disk.
      - uses: actions/checkout@v4
        with:
          repository: juijs-vue/jui-graph-ts
          path: jui-graph-ts

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: |
            jui-chart-vue/package-lock.json
            jui-graph-ts/package-lock.json

      - name: Build jui-graph-ts (local dependency)
        working-directory: jui-graph-ts
        run: |
          npm ci
          npm run build:lib

      - name: Install jui-chart-vue
        working-directory: jui-chart-vue
        run: npm ci

      - name: Build demo site
        working-directory: jui-chart-vue
        run: npm run build

      - uses: actions/upload-pages-artifact@v3
        with:
          path: jui-chart-vue/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
